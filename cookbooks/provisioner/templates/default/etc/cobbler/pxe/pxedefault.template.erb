<%= node['provisioner']['file_header'] %>

DEFAULT menu
PROMPT 0
MENU TITLE <%= node['linux']['organization'] %> build service
TIMEOUT <%= node['provisioner']['cobbler']['pxe_timeout'] %>
TOTALTIMEOUT 6000
MENU MASTER PASSWD <%= @root_hash %>
ONTIMEOUT $pxe_timeout_profile

LABEL local
        MENU LABEL (local)
        MENU DEFAULT
        LOCALBOOT -1

$pxe_menu_items

MENU end
